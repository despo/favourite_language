inherit_from: .rubocop_todo.yml

require:
  - rubocop-rails
  - rubocop-rspec

AllCops:
  NewCops: enable
  Exclude:
    - 'bin/*'
    - 'config/initializers/simple_form*.rb'
    - 'node_modules/**/**/**/*'

Metrics/BlockLength:
  Exclude:
    - 'spec/**/*.rb'
